{
  "name": "perpetual-calendar",
  "version": "0.1.0",
  "private": true,
  "description": "壽星萬年曆現代化改寫版（原 sxwnl 5.10.3 移植）",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve .",
    "build": "node scripts/build.mjs",
    "build:watch": "node scripts/build.mjs --watch",
    "build:meta": "node scripts/build.mjs --meta",
    "preview": "npx --yes serve dist",
    "test": "node --test tests/*.test.js",
    "golden": "node scripts/generate-golden.mjs",
    "docify": "node scripts/docify-legacy.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
